services:
  db.paysim:
    image: mysql:8
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 123
      MYSQL_DATABASE: paysim
    ports:
      - 3308:3306
    volumes:
      - mysql.paysim:/var/lib/mysql

volumes:
  mysql.paysim: